normal(title:"Polygons"){
    body{
        grid((6,3),columns-weight:[1,3]){
            styles:{
                "*": {margin:2}
                source:{
                    at:left
                }
            }
            // Row
            group(background:white,draw-contour){
                pentagon(at:center, size:50, background:themeColors[4], color:themeColors[2])
            }
            source(ntexup, """
                pentagon()
                """)

            // Row
            group(background:white,draw-contour){
                hexagon(at:center, size:50, background:themeColors[4], color:themeColors[2])
            }
            source(ntexup, """
                hexagon()
                """)

            // Row
            group(background:white,draw-contour){
                heptagon(at:center, size:50, background:themeColors[4], color:themeColors[2])
            }
            source(ntexup, """
                heptagon()
                """)


            // Row
            group(background:white,draw-contour){
                octagon(at:center, size:50, background:themeColors[4], color:themeColors[2])
            }
            source(ntexup, """
                octagon()
                """)

            // Row
            group(background:white,draw-contour){
                nonagon(at:center, size:50, background:themeColors[4], color:themeColors[2])
            }
            source(ntexup, """
                nonagon()
                """)

            // Row
            group(background:white,draw-contour){
                decagon(at:center, size:50, background:themeColors[4], color:themeColors[2])
            }
            source(ntexup, """
                decagon()
                """)

            // Row
            group(background:white,draw-contour){
                polygon(at:center, size:50, background:themeColors[4], color:themeColors[2])
            }
            source(ntexup, """
                polygon()
                """)

            // Row
            group(background:white,draw-contour){
                polygon(at:center, size:50, count:8, background:themeColors[4], color:themeColors[2])
            }
            source(ntexup, """
                polygon(count:8)
                """)

            // Row
            group(background:white,draw-contour){
                polygon(at:center, size:50, points:[(0,0),(50,0),(100,20),(80,80)], background:themeColors[4], color:themeColors[2])
            }
            source(ntexup, """
                polygon(points:[
                (0,0),(50,0),
                (100,80),(50,50)
                ])
                """)

        }
    }

}
